general:
  artifacts:
    - "build/main.css"
    - "build/main-min.css"
machine:
  node:
    version: 4.4.7
dependencies:
  pre:
    - npm install npm -g
    - npm install
test:
  override:
    - npm test
    - npm run build
