general:
  artifacts:
    - "build/main.css"
    - "build/main-min.css"
    - "build/main-stats.json"
machine:
  node:
    version: 4.4.7
dependencies:
  pre:
    - npm install npm -g
    - npm install
test:
  override:
    - npm test
    - npm run build
deployment:
  staging:
    branch: igloo-heroku-setup
    heroku:
      appname: igloocss
