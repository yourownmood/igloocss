general:
  artifacts:
    - "build/main.css"
    - "build/main-min.css"
    - "build/main-stats.json"
machine:
  node:
    version: 7.4.0
test:
  override:
    - npm test
    - npm run build
deployment:
  staging:
    branch: master
    heroku:
      appname: igloocss
